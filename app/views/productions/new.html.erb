<h1>New production</h1>

<% form_for([@theater,@production]) do |f| %>
  <%= render :partial => 'form', :locals=>{:f=>f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', theater_productions_path(@theater) %>