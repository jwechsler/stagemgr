<h1>Editing production</h1>

<div class="grid-x grid-padding-x">
  <div class="small-12 cell">
    <%= form_for([@production.theater, @production]) do |f| %>
      <%= render :partial => 'form', :locals=>{:f=>f} %>
      <p>
        <%= f.submit 'Update' %>
      </p>
    <% end %>

  </div>
</div>
