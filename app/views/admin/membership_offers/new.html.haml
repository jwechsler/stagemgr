= content_for :title do
  New Offer
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: 'Membership Offers', path: admin_membership_offers_path }, { label: "New" }]

= render 'form', :membership_offer=>@membership_offer
