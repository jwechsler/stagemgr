<%= form_for(@admin_report) do |f| %>
  <% if @admin_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_report.errors.count, "error") %> prohibited this admin_report from being saved:</h2>

      <ul>
      <% @admin_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
