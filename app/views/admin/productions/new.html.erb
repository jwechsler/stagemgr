<h1>New production</h1>

<%= semantic_form_for([:admin,@theater,@production], :html=>{:multipart=>true}) do |f| %>
  <%= render :partial => '/productions/form', :locals=>{:f=>f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', edit_admin_theater_path(@theater) %>
