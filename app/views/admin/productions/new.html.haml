.grid-x.grid-padding-x
  .cell
    = content_for :title do
      New Production
    = content_for :breadcrumbs do
      = render "shared/components/breadcrumb", items: [{ label: 'Theaters', path: admin_theaters_path }, { label: @production.theater.name, path: admin_theater_path(@production.theater) }, { label: "New Production" }]
  .cell
    =render :partial=>'/admin/productions/form', :locals=>{:production=>@production}
