<h1>New ticket_class</h1>

<%= form_for([:admin,@theater,@production,@ticket_class]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals=>{:f=>f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', admin_theater_production_path(@production.theater, @production)  %>
