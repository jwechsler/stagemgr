= content_for :title do
  New Festival
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: 'Festivals', path: admin_festivals_path }, { label: "New Festival" }]

.grid-x.grid-padding-x
  .small-12.cell
    = render 'form', festival: @festival
