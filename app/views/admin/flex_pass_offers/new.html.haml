= content_for :title do
  New Flex Pass Offer
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: 'Flex Pass Offers', path: admin_flex_pass_offers_path }, { label: "New Offer" }]

.grid-x.grid-padding-x
  .small-12.cell
    = render 'form', :flex_pass_offer=>@flex_pass_offer
