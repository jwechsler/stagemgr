= content_for :title do
  = @flex_pass_offer.name
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: 'Flex Pass Offers', path: admin_flex_pass_offers_path }, { label: @flex_pass_offer.name }]

.grid-x.grid-padding-x
  .small-12.cell
    = render 'form', :flex_pass_offer=>@flex_pass_offer
