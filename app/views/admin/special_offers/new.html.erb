<h1>New Special Offer for <%=@context.type%> (<%=@context.id%>)</h1>

<% form_for(@rest_path + [@special_offer]) do |f| %>
  <%= render :partial => '/admin/special_offers/form', :locals=>{:f=>f} %>
<p>
  <%= f.submit 'Create' %>
</p>
<% end %>

<%= link_to 'Back', url_for(@rest_path) %>