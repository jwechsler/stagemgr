<h1>New Special Offer</h1>

<% form_for([:admin, @special_offer]) do |f| %>
  <%= render :partial => '/admin/special_offers/form', :locals=>{:f=>f} %>
<p>
  <%= f.submit 'Create' %>
</p>
<% end %>

<%= link_to 'Back', admin_special_offers_path %>