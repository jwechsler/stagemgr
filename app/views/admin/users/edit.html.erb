<h1>Edit User</h1>

<%= semantic_form_for [:admin,@user] do |f| %>
	<%= f.error_messages %>
	<%= render :partial => "form", :locals=>{:f=>f} %>
	<%= f.submit "Update" %>
<% end %>
<br /><%= link_to "Back", admin_users_path %>
