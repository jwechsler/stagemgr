= content_for :title do
  New User
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: 'Users', path: admin_users_path }]
    %li.current New

.grid-x
  .small-12.cell
    = render 'form', :user=>@user
