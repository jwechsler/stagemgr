<%= simple_form_for [:admin, @venue] do |f| %>
    <%= token_tag %>
    <%= f.input :name %>
    <%= f.input :ordinal_sort %>
    <%= f.input :external, as: :boolean, label: "External venue" %>
    <%= f.button :submit %>

<%- end %>
