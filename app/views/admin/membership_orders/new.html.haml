= content_for :title do
  New Membership Order
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: 'Membership Offers', path: admin_membership_offers_path }, { label: "New Order" }]

= render '/admin/membership_orders/form'
