= content_for :title do
  Edit Membership Order
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: 'Membership Offers', path: admin_membership_offers_path }, { label: "Order #{@membership_order.id}" }]

= render '/admin/membership_orders/form'
