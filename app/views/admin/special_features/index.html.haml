= content_for :title do
  Special Features
= content_for :breadcrumbs do
  = render "shared/components/breadcrumb", items: [{ label: "Special Features" }]

.grid-x.grid-padding-x
  .small-12.cell
    %table#special-feature-listing{:data=>{:source=>"#{admin_special_features_path(format: :json)}"}}
      %thead
        %tr
          %th Name
          %th Description
          %th Status
          %th Actions

.grid-x.grid-padding-x
  .small-12.cell
    = ui_button "New Special Feature", new_admin_special_feature_path, size: :small, class: 'right'

= content_for :js do
  :javascript
    $(document).ready(function() {
      $('#special-feature-listing').dataTable({
          "processing": true,
          "serverSide": true,
          "stateSave": true,
          "ajax": $('#theater-listing').data('source'),
          "pagingType": 'full_numbers',
          "language": {
            "lengthMenu" : "_MENU_",
            "search": "_INPUT_"
          },
          columns: [
            {data: 'name'},
            {data: 'description'},
            {data: 'status'},
            {data: 'actions'},
          ],
          "order": [ [0, "asc"], [2, "asc"]],
        });
      });

      // setupActiveSwitchOnDataTable('#special-feature-listing',2)

