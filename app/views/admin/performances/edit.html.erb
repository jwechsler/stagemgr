<h1>Editing performance</h1>

<% form_for([:admin,@performance.production.theater,@performance.production,@performance]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals=>{:f=>f}%>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Back', admin_theater_production_path(@performance.production.theater,@performance.production) %>