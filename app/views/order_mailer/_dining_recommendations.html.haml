-# Where to eat before the show. Deliberately empty: a ticketing system has no
-# opinion about your neighbourhood, and a generic "there are restaurants
-# nearby" paragraph is worse than silence.
-#
-# Houses that want to recommend somewhere override this partial in their theme
-# (sites/<slug>/views/order_mailer/_dining_recommendations.html.haml); the
-# heading belongs in the override, so that a house with nothing to say prints
-# no heading either.
