-# The bar, the coat check, the lobby -- whatever this house wants patrons to
-# know is waiting for them. Empty by default for the same reason as
-# _dining_recommendations: nothing generic is true of every venue.
-#
-# Extracted out of _performance_info so that a house can add a paragraph here
-# without overriding (and then having to maintain) that partial's structure.
