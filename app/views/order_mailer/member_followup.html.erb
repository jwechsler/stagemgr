<%= render 'followup_title', order: @order %>
<%# Members always get the house's own letter, whoever produced the show. %>
<p>
  As a member, you are a core part of our community, both as audience and supporters of small theatre.
</p>
<%= render 'core_followup', order: @order %>
