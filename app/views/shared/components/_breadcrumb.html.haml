-# Foundation 6 breadcrumb wrapped as a partial.
-#
-# Usage:
-#   = render "shared/components/breadcrumb", items: [
-#       { label: "Special Features", path: admin_special_features_path },
-#       { label: "Edit" }
-#     ]
-#
-# Parent items are rendered as real links via their :path.
-# The last item is always rendered with .current and `<a href='#'>` — the
-# Foundation convention where `#` marks the current page (a non-functional
-# anchor). Any :path on the last item is ignored.

%ul.breadcrumbs
  - items.each_with_index do |item, idx|
    - if idx == items.length - 1
      %li.current
        %a{href: '#'}= item[:label]
    - else
      %li= link_to(item[:label], item[:path])
