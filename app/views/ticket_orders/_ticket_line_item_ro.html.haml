- reserved_seating = f.object.order&.performance&.production&.has_reserved_seating?
- initial_count = f.object.ticket_count.to_i
- hide_row = reserved_seating && initial_count <= 0
.grid-x.grid-padding-x.ticket_line_item.line_item{:price=>f.object.ticket_class.try(:ticket_price), :ticket_type=>f.object.ticket_class.try(:ticket_type), "data-ticket-class-id" => f.object.ticket_class_id, :style => hide_row ? "display:none" : ""}
  .small-6.cell.vertical-center
    = f.hidden_field :ticket_class_id
    = f.object.ticket_class.class_name
    - unless f.object.ticket_class.purchase_page_annotation.blank?
      .span.ticket_class_annotation
        = display_markdown(f.object.ticket_class.purchase_page_annotation)
  .small-6.medium-3.cell
    - f.object.ticket_count = 0 if f.object.ticket_count.nil?
    = f.hidden_field :ticket_count, :class=>"ticket_count_control ticket_class_count_#{f.object.ticket_class_id}"
    %span{id:"ticket_class_qty_display_#{f.object.ticket_class_id}", class:'ticket_count'}
      = f.object.ticket_count
  .small-12.medium-3.cell.vertical-center
    - if f.object.ticket_class.software_managed? || f.object.ticket_class.hide_pricing?
      n/a
    -else
      - effective_price = f.object.price_override.present? ? f.object.price_override : f.object.ticket_class.ticket_price
      %span.ticket_price= number_to_currency(effective_price)
