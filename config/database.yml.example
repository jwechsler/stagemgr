# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql2
  encoding: utf8mb4
  collation: utf8mb4_0900_ai_ci
  database: stagemgr_development
  username: root
  password:
  socket: /tmp/mysql.sock

# The test/cucumber suites run on MySQL (not sqlite). Values are ENV-driven so
# the same template works locally and in CI (which sets DATABASE_HOST etc. to
# point at its ephemeral MySQL service).
# Do not set this db to the same as development or production.
test: &TEST
  adapter: mysql2
  encoding: utf8mb4
  collation: utf8mb4_0900_ai_ci
  database: <%= ENV.fetch("TEST_DATABASE", "stagemgr_test") %>
  username: <%= ENV.fetch("DATABASE_USERNAME", "root") %>
  password: <%= ENV.fetch("DATABASE_PASSWORD", "") %>
  host: <%= ENV.fetch("DATABASE_HOST", "127.0.0.1") %>
  port: <%= ENV.fetch("DATABASE_PORT", 3306) %>
  pool: 5

# Unused stub -- the real production config is generated outside this repo and
# lives on the server (config/database.yml is gitignored). It must also set
# `encoding: utf8mb4` and `collation: utf8mb4_0900_ai_ci`; see
# docs/runbooks/utf8mb4-migration.md.
production:
  adapter: sqlite3
  database: db/production.sqlite3
  pool: 5
  timeout: 5000

cucumber:
  <<: *TEST
